const suma = (num1, num2) => {
  return num1 + num2;
};

const resta = (num1, num2) => {
    return num1 - num2;
  };

  const mult = (num1, num2) => {
    return num1 * num2;
  };

  module.exports = {suma,resta,mult}//no usar  exports={suma,resta,mult} esto porque resulta mas trabajoso
  